
let a = 4
let promptData = parseInt(prompt("Enter Value: "))

switch(a){
    case promptData:
    console.log(`a is equal ${promptData}`);
    break;
    case promptData:
        console.log(`a is equal ${promptData}`);
        break;
    case promptData:
        console.log(`a is equal ${promptData}`);
        break;
    case promptData:
        console.log(`a is equal ${promptData}`);
        break;
    default:
        console.log("Default code runs here");
}